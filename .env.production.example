# Domain & TLS
DOMAIN=vigooth.com
ACME_EMAIL=admin@vigooth.com

# PostgreSQL
POSTGRES_USER=vigooth
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=vigooth

# API Security
# Generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_ME

# Admin — accounts (by email) allowed into the portal's /admin space (comma-separated)
ADMIN_EMAILS=
